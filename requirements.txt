# Sync with [project.dependencies] in pyproject.toml (for hosts that only run pip install -r)
fastapi>=0.115
uvicorn[standard]>=0.32
aiogram>=3.15
sqlalchemy[asyncio]>=2.0.36
aiosqlite>=0.20
alembic>=1.14
apscheduler>=3.10.4
pydantic-settings>=2.6
PyYAML>=6.0
